# 1.3.1
  - Do not throw errors on pages which do not have a body tag

# 1.3.0
  - Add a button on the options page to go to the feedback page
  - Add a button on the options page to go to the ratings page

# 1.2.0
  - Run extension on `document_end` instead of `document_idle`.

# 1.1.0
  - Allow testing on the settings page.

# 1.0.0
  - Reuse open windows if the plugin is allowed incognito.

# 0.2.0
  - Handle clicks on elements inside anchor tags.
  - If the website wouldn't have opened a link, neither will this plugin.

# 0.1.2
  - Fix a ctrl/alt key bug.

# 0.1.1
  - Improve copy.

# 0.1.0
  - Add settings page.
  - Require `storage` permission.
